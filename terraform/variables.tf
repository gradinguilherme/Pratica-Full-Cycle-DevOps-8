variable "file_name" {
  # definir um nome "default" para o arquivo a ser criado.
  default = "example.txt"
}

variable "file_content" {
  # definir um conteúdo "default" para o arquivo a ser criado.
  default = "Here we will have the content for the text file"
}